<!-- 文章标签列表 -->

<% if (item.tags && item.tags.length) { %>
    <div class="tags">
        <% item.tags.forEach(function (tag) { %>
            <a href="<%= url_for(tag.path) %>" class="ywh_button small"><%= tag.name %></a>
        <% }); %>
    </div>
<% } %>
